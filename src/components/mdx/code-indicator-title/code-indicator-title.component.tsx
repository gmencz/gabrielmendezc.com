import React from 'react';
import * as SC from './code-indicator-title.styles';

export const CodeIndicatorTitle: React.FC = ({ children }) => {
  return <SC.CodeIndicatorTitle>{children}</SC.CodeIndicatorTitle>;
};
